const state = {
    userLoggedIn: false
}

const getters = {
    isUserLoggedIn(state) {
        return state.userLoggedIn
    }
}

const actions = {
    // [ACTION_NAME]()
}

export default {
    getters
}