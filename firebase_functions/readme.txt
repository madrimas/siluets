To deploy functions to firestore please run in terminal:

npm install (node_modules required)
firebase deploy --only functions

in firebase_functions directory

It will deploy index.js functions to firestore